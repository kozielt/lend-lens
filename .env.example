# Optional: Aave GraphQL endpoint (server-only). Set to http://127.0.0.1:1 to simulate an outage.
AAVE_API_URL=https://api.v3.aave.com/graphql
RPC_URL=https://ethereum.publicnode.com
# Optional: if set, POST /api/revalidate requires "Authorization: Bearer <secret>"
REVALIDATE_SECRET=
